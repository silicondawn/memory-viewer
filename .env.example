# Memory Viewer Configuration
# Copy this file to .env and customize the values

# Server port (default: 8901)
PORT=8901

# Workspace directory containing .md files
# This is the path INSIDE the container
# Mount your host directory to this path via docker-compose volumes
WORKSPACE_DIR=/app/workspace

# Static assets directory (built frontend)
STATIC_DIR=/app/dist

# Node environment (production or development)
NODE_ENV=production
